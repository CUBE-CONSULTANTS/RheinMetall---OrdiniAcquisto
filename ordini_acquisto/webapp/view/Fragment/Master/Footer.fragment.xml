<c:FragmentDefinition
	xmlns="sap.m"
	xmlns:c="sap.ui.core"
>
<OverflowToolbar >    
  <ToolbarSpacer/>
  <!-- <Button
  visible="{= ${userModel>/tipoUtente} === 'Interno' ? false : true }"
  icon="sap-icon://request"
  text="{i18n>Richiedi Modifica}"
  type="Emphasized"    
  /> -->
  <!-- <Button
  visible="{= ${userModel>/tipoUtente} === 'Interno' ? true : false }"
  icon="sap-icon://save"
  text="{i18n>Conferma OdA}"
  type="Emphasized"    
/> -->
  </OverflowToolbar>
</c:FragmentDefinition>